# Root package.json dependencies for Vercel API routes
# These are needed for the serverless functions in /api directory

# Command to install:
# npm install

# This installs all dependencies needed for both frontend and API routes
